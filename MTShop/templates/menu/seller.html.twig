<nav class="navbar navbar-expand-lg navbar-mtshop shadow-sm" data-turbo="false">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('app_home_page') }}" data-turbo="false">MTShop</a>
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#mainNavbar"
            aria-controls="mainNavbar"
            aria-expanded="false"
            aria-label="Open navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'app_seller_dashboard' %}active{% endif %}" href="{{ path('app_seller_dashboard') }}" data-turbo="false">Mes produits</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.request.attributes.get('_route') == 'app_my_account' %}active{% endif %}" href="{{ path('app_my_account') }}" data-turbo="false">Mon compte</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}" data-turbo="false">Se déconnecter</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
